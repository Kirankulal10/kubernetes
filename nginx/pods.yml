apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: nginx-namespace
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
  tolerations:
  - key: "key"
    operator: "Equal"
    value: "val"
    effect: "NoSchedule" 
