apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: my-pvc
  namespace: deployment

spec:
  accessModes:
  - ReadWriteOnce

  storageClassName: local-storage

  resources:
    requests:
      storage: 1Gi
